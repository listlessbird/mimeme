services:
  postgres:
    image: postgres:16-alpine
    container_name: mimeme-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: mimeme
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U postgres" ]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    container_name: mimeme-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes
    healthcheck:
      test: [ "CMD", "redis-cli", "ping" ]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  minio:
    image: minio/minio:latest
    container_name: mimeme-minio
    ports:
      - "9000:9000"
      - "9001:9001"
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    volumes:
      - minio_data:/data
    command: server /data --console-address ":9001"
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:9000/minio/health/live" ]
      interval: 30s
      timeout: 10s
      retries: 3
    restart: unless-stopped

  minio-init:
    image: minio/mc:latest
    depends_on:
      minio:
        condition: service_healthy
    entrypoint: >
      /bin/sh -c "
      mc alias set local http://minio:9000 minioadmin minioadmin;
      mc mb --ignore-existing local/mimeme-media;
      mc mb --ignore-existing local/mimeme-artifacts;
      mc anonymous set download local/mimeme-media;
      mc anonymous set none local/mimeme-artifacts;
      "

  temporal:
    image: temporalio/auto-setup:latest
    container_name: mimeme-temporal
    ports:
      - "7233:7233"
    environment:
      - DB=postgres12
      - DB_PORT=5432
      - POSTGRES_USER=postgres
      - POSTGRES_PWD=postgres
      - POSTGRES_SEEDS=postgres
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

  temporal-ui:
    image: temporalio/ui:latest
    container_name: mimeme-temporal-ui
    ports:
      - "8088:8080"
    environment:
      - TEMPORAL_ADDRESS=temporal:7233
      - TEMPORAL_CORS_ORIGINS=http://localhost:3000
    depends_on:
      - temporal
    restart: unless-stopped

  api:
    build:
      context: .
      dockerfile: Dockerfile.cpu
      target: api
    container_name: mimeme-api
    ports:
      - "8000:8000"
    environment:
      - APP_ENV=development
      - DEBUG=true
      - LOG_LEVEL=INFO
      - DB_URL=postgresql://postgres:postgres@postgres:5432/mimeme
      - TEMPORAL_HOST=temporal:7233
      - MEDIA_S3_ENDPOINT_URL=http://minio:9000
      - MEDIA_S3_BUCKET=mimeme-media
      - MEDIA_S3_ACCESS_KEY_ID=minioadmin
      - MEDIA_S3_SECRET_ACCESS_KEY=minioadmin
      - MEDIA_PUBLIC_BASE_URL=http://localhost:9000/mimeme-media
      - ARTIFACT_S3_ENDPOINT_URL=http://minio:9000
      - ARTIFACT_S3_BUCKET=mimeme-artifacts
      - ARTIFACT_S3_ACCESS_KEY_ID=minioadmin
      - ARTIFACT_S3_SECRET_ACCESS_KEY=minioadmin
    volumes:
      - ./data:/app/data
      - huggingface_cache:/app/.cache/huggingface
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      temporal:
        condition: service_started
      minio:
        condition: service_healthy
      minio-init:
        condition: service_completed_successfully
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:8000/ready" ]
      interval: 30s
      timeout: 10s
      retries: 3
    restart: unless-stopped

  worker:
    build:
      context: .
      dockerfile: Dockerfile.cpu
      target: worker
    container_name: mimeme-worker
    command: python -m workers.worker
    environment:
      - APP_ENV=development
      - LOG_LEVEL=INFO
      - DB_URL=postgresql://postgres:postgres@postgres:5432/mimeme
      - TEMPORAL_HOST=temporal:7233
      - MEDIA_S3_ENDPOINT_URL=http://minio:9000
      - MEDIA_S3_BUCKET=mimeme-media
      - MEDIA_S3_ACCESS_KEY_ID=minioadmin
      - MEDIA_S3_SECRET_ACCESS_KEY=minioadmin
      - ARTIFACT_S3_ENDPOINT_URL=http://minio:9000
      - ARTIFACT_S3_BUCKET=mimeme-artifacts
      - ARTIFACT_S3_ACCESS_KEY_ID=minioadmin
      - ARTIFACT_S3_SECRET_ACCESS_KEY=minioadmin
      - EMBED_DEVICE=cpu
    volumes:
      - ./data:/app/data
      - huggingface_cache:/app/.cache/huggingface
    depends_on:
      postgres:
        condition: service_healthy
      temporal:
        condition: service_started
      minio:
        condition: service_healthy
      minio-init:
        condition: service_completed_successfully
    restart: unless-stopped

volumes:
  postgres_data:
  redis_data:
  minio_data:
  huggingface_cache:


networks:
  default:
    name: mimeme-network
